# Codex 红绿灯实施计划

> **给执行代理的要求：** 实施本计划时必须使用 `superpowers:subagent-driven-development` 或 `superpowers:executing-plans`。每个步骤使用复选框跟踪进度。

**目标：** 做一个纯本地 Windows WPF EXE，用悬浮红黄绿灯显示 Codex CLI 的运行状态。

**架构：** 项目拆成可测试的核心库和 WPF 壳程序。Codex hooks 负责写入本地 JSON 状态文件，WPF 程序监听该文件并更新灯色，设置和统计数据保存在 Codex 配置目录；优先使用 `CODEX_HOME`，否则使用 `%USERPROFILE%\.codex`。

**技术栈：** .NET 8、C# 12、WPF、xUnit、`System.Text.Json`、`System.Windows.Forms.NotifyIcon`。

---

## 文件结构

- 创建 `CodexTrafficLight.sln`：解决方案文件。
- 创建 `src/CodexTrafficLight.Core/CodexTrafficLight.Core.csproj`：可测试的核心逻辑。
- 创建 `src/CodexTrafficLight.Core/Models/CodexLightState.cs`：灯色状态枚举。
- 创建 `src/CodexTrafficLight.Core/Models/CodexStatus.cs`：状态文件模型。
- 创建 `src/CodexTrafficLight.Core/Models/AppSettings.cs`：界面设置模型。
- 创建 `src/CodexTrafficLight.Core/Models/DailyStats.cs`：每日统计模型。
- 创建 `src/CodexTrafficLight.Core/Services/CodexPaths.cs`：统一管理 Codex 配置目录；优先使用 `CODEX_HOME`，否则使用 `%USERPROFILE%\.codex`。
- 创建 `src/CodexTrafficLight.Core/Services/StatusFileStore.cs`：读写当前状态。
- 创建 `src/CodexTrafficLight.Core/Services/AppSettingsStore.cs`：读写窗口位置、主题、静音和样式。
- 创建 `src/CodexTrafficLight.Core/Services/StatsStore.cs`：记录本地周报数据。
- 创建 `src/CodexTrafficLight.Core/Services/CodexHookInstaller.cs`：把本工具 hooks 合并进 `hooks.json`。
- 创建 `src/CodexTrafficLight.App/CodexTrafficLight.App.csproj`：WPF 可执行程序。
- 创建 `src/CodexTrafficLight.App/App.xaml`：WPF 应用入口。
- 创建 `src/CodexTrafficLight.App/App.xaml.cs`：启动和退出逻辑。
- 创建 `src/CodexTrafficLight.App/MainWindow.xaml`：红绿灯界面。
- 创建 `src/CodexTrafficLight.App/MainWindow.xaml.cs`：窗口逻辑、托盘菜单、文件监听、动画。
- 创建 `src/CodexTrafficLight.App/StatusFileWatcher.cs`：WPF 使用的状态文件监听封装。
- 创建 `tests/CodexTrafficLight.Tests/CodexTrafficLight.Tests.csproj`：xUnit 测试项目。
- 创建 `tests/CodexTrafficLight.Tests/StatusFileStoreTests.cs`：状态读写测试。
- 创建 `tests/CodexTrafficLight.Tests/AppSettingsStoreTests.cs`：设置持久化测试。
- 创建 `tests/CodexTrafficLight.Tests/StatsStoreTests.cs`：统计逻辑测试。
- 创建 `tests/CodexTrafficLight.Tests/CodexHookInstallerTests.cs`：hooks 合并测试。
- 创建 `README.md`：本地运行、打包和 hook 信任说明。

## 任务 1：创建解决方案骨架

**涉及文件：**

- 创建 `CodexTrafficLight.sln`
- 创建 `src/CodexTrafficLight.Core/CodexTrafficLight.Core.csproj`
- 创建 `src/CodexTrafficLight.App/CodexTrafficLight.App.csproj`
- 创建 `tests/CodexTrafficLight.Tests/CodexTrafficLight.Tests.csproj`

- [ ] **步骤 1：创建解决方案和项目**

运行：

```powershell
dotnet new sln -n CodexTrafficLight
dotnet new classlib -n CodexTrafficLight.Core -o src/CodexTrafficLight.Core -f net8.0
dotnet new wpf -n CodexTrafficLight.App -o src/CodexTrafficLight.App -f net8.0-windows
dotnet new xunit -n CodexTrafficLight.Tests -o tests/CodexTrafficLight.Tests -f net8.0
dotnet sln CodexTrafficLight.sln add src/CodexTrafficLight.Core/CodexTrafficLight.Core.csproj
dotnet sln CodexTrafficLight.sln add src/CodexTrafficLight.App/CodexTrafficLight.App.csproj
dotnet sln CodexTrafficLight.sln add tests/CodexTrafficLight.Tests/CodexTrafficLight.Tests.csproj
dotnet add src/CodexTrafficLight.App/CodexTrafficLight.App.csproj reference src/CodexTrafficLight.Core/CodexTrafficLight.Core.csproj
dotnet add tests/CodexTrafficLight.Tests/CodexTrafficLight.Tests.csproj reference src/CodexTrafficLight.Core/CodexTrafficLight.Core.csproj
```

预期结果：所有命令退出码为 0。

- [ ] **步骤 2：调整 WPF 项目配置**

把 `src/CodexTrafficLight.App/CodexTrafficLight.App.csproj` 设置为：

```xml
<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net8.0-windows</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <UseWPF>true</UseWPF>
    <UseWindowsForms>true</UseWindowsForms>
  </PropertyGroup>
  <ItemGroup>
    <ProjectReference Include="..\CodexTrafficLight.Core\CodexTrafficLight.Core.csproj" />
  </ItemGroup>
</Project>
```

- [ ] **步骤 3：初次构建**

运行：

```powershell
dotnet build CodexTrafficLight.sln
```

预期结果：构建成功。

## 任务 2：添加核心模型和路径服务

**涉及文件：**

- 创建 `src/CodexTrafficLight.Core/Models/CodexLightState.cs`
- 创建 `src/CodexTrafficLight.Core/Models/CodexStatus.cs`
- 创建 `src/CodexTrafficLight.Core/Models/AppSettings.cs`
- 创建 `src/CodexTrafficLight.Core/Models/DailyStats.cs`
- 创建 `src/CodexTrafficLight.Core/Services/CodexPaths.cs`

- [ ] **步骤 1：创建灯色状态枚举**

```csharp
namespace CodexTrafficLight.Core.Models;

public enum CodexLightState
{
    Unknown = 0,
    Red,
    Yellow,
    Green
}
```

- [ ] **步骤 2：创建状态模型**

```csharp
namespace CodexTrafficLight.Core.Models;

public sealed record CodexStatus(
    CodexLightState State,
    string Event,
    DateTimeOffset UpdatedAt);
```

- [ ] **步骤 3：创建应用设置模型**

```csharp
namespace CodexTrafficLight.Core.Models;

public sealed record AppSettings
{
    public double? WindowLeft { get; init; }
    public double? WindowTop { get; init; }
    public string Theme { get; init; } = "dark";
    public string Style { get; init; } = "triple";
    public bool Muted { get; init; }
}
```

- [ ] **步骤 4：创建每日统计模型**

```csharp
namespace CodexTrafficLight.Core.Models;

public sealed record DailyStats
{
    public int RedCount { get; init; }
    public int GreenCount { get; init; }
    public long RedDurationMs { get; init; }
}
```

- [ ] **步骤 5：创建路径服务**

```csharp
namespace CodexTrafficLight.Core.Services;

public sealed class CodexPaths
{
    public CodexPaths(string? homeDirectory = null)
    {
        HomeDirectory = string.IsNullOrWhiteSpace(homeDirectory)
            ? Environment.GetFolderPath(Environment.SpecialFolder.UserProfile)
            : homeDirectory;

        var codexHome = string.IsNullOrWhiteSpace(homeDirectory)
            ? Environment.GetEnvironmentVariable("CODEX_HOME")
            : null;

        CodexDirectory = string.IsNullOrWhiteSpace(codexHome)
            ? Path.Combine(HomeDirectory, ".codex")
            : codexHome;
        HooksPath = Path.Combine(CodexDirectory, "hooks.json");
        StatusPath = Path.Combine(CodexDirectory, "codex_traffic_light_state.json");
        SettingsPath = Path.Combine(CodexDirectory, "codex_traffic_light_settings.json");
        StatsPath = Path.Combine(CodexDirectory, "codex_traffic_light_stats.json");
        HookScriptDirectory = Path.Combine(CodexDirectory, "codex-traffic-light");
        HookScriptPath = Path.Combine(HookScriptDirectory, "codex_traffic_light_write_status.ps1");
    }

    public string HomeDirectory { get; }
    public string CodexDirectory { get; }
    public string HooksPath { get; }
    public string StatusPath { get; }
    public string SettingsPath { get; }
    public string StatsPath { get; }
    public string HookScriptDirectory { get; }
    public string HookScriptPath { get; }

    public void EnsureCodexDirectory()
    {
        Directory.CreateDirectory(CodexDirectory);
    }
}
```

- [ ] **步骤 6：构建验证**

运行：

```powershell
dotnet build CodexTrafficLight.sln
```

预期结果：构建成功。

## 任务 3：实现状态文件读写

**涉及文件：**

- 创建 `src/CodexTrafficLight.Core/Services/StatusFileStore.cs`
- 创建 `tests/CodexTrafficLight.Tests/StatusFileStoreTests.cs`

- [ ] **步骤 1：先写失败测试**

```csharp
using CodexTrafficLight.Core.Models;
using CodexTrafficLight.Core.Services;

namespace CodexTrafficLight.Tests;

public sealed class StatusFileStoreTests
{
    [Fact]
    public void WriteAndReadStatusRoundTrips()
    {
        var root = CreateTempRoot();
        var store = new StatusFileStore(new CodexPaths(root));

        var status = new CodexStatus(CodexLightState.Red, "UserPromptSubmit", DateTimeOffset.Parse("2026-06-01T10:45:00+08:00"));
        store.Write(status);

        var actual = store.Read();

        Assert.NotNull(actual);
        Assert.Equal(CodexLightState.Red, actual.State);
        Assert.Equal("UserPromptSubmit", actual.Event);
        Assert.Equal(status.UpdatedAt, actual.UpdatedAt);
    }

    [Fact]
    public void ReadReturnsUnknownWhenFileIsMissing()
    {
        var root = CreateTempRoot();
        var store = new StatusFileStore(new CodexPaths(root));

        var actual = store.Read();

        Assert.Equal(CodexLightState.Unknown, actual.State);
        Assert.Equal("missing", actual.Event);
    }

    private static string CreateTempRoot()
    {
        var path = Path.Combine(Path.GetTempPath(), "CodexTrafficLightTests", Guid.NewGuid().ToString("N"));
        Directory.CreateDirectory(path);
        return path;
    }
}
```

- [ ] **步骤 2：运行测试确认失败**

运行：

```powershell
dotnet test tests/CodexTrafficLight.Tests/CodexTrafficLight.Tests.csproj --filter StatusFileStoreTests
```

预期结果：测试失败，因为 `StatusFileStore` 尚未实现。

- [ ] **步骤 3：实现状态文件读写**

```csharp
using System.Text.Json;
using System.Text.Json.Serialization;
using CodexTrafficLight.Core.Models;

namespace CodexTrafficLight.Core.Services;

public sealed class StatusFileStore
{
    private static readonly JsonSerializerOptions JsonOptions = new()
    {
        WriteIndented = true,
        PropertyNamingPolicy = JsonNamingPolicy.CamelCase,
        Converters = { new JsonStringEnumConverter(JsonNamingPolicy.CamelCase) }
    };

    private readonly CodexPaths _paths;

    public StatusFileStore(CodexPaths paths)
    {
        _paths = paths;
    }

    public CodexStatus Read()
    {
        try
        {
            if (!File.Exists(_paths.StatusPath))
            {
                return new CodexStatus(CodexLightState.Unknown, "missing", DateTimeOffset.Now);
            }

            var json = File.ReadAllText(_paths.StatusPath);
            return JsonSerializer.Deserialize<CodexStatus>(json, JsonOptions)
                ?? new CodexStatus(CodexLightState.Unknown, "invalid", DateTimeOffset.Now);
        }
        catch
        {
            return new CodexStatus(CodexLightState.Unknown, "error", DateTimeOffset.Now);
        }
    }

    public void Write(CodexStatus status)
    {
        _paths.EnsureCodexDirectory();
        var tempPath = _paths.StatusPath + ".tmp";
        var json = JsonSerializer.Serialize(status, JsonOptions);
        File.WriteAllText(tempPath, json);
        File.Move(tempPath, _paths.StatusPath, overwrite: true);
    }

    public void Write(CodexLightState state, string eventName)
    {
        Write(new CodexStatus(state, eventName, DateTimeOffset.Now));
    }
}
```

- [ ] **步骤 4：运行测试确认通过**

运行：

```powershell
dotnet test tests/CodexTrafficLight.Tests/CodexTrafficLight.Tests.csproj --filter StatusFileStoreTests
```

预期结果：测试通过。

## 任务 4：实现设置持久化

**涉及文件：**

- 创建 `src/CodexTrafficLight.Core/Services/AppSettingsStore.cs`
- 创建 `tests/CodexTrafficLight.Tests/AppSettingsStoreTests.cs`

- [ ] **步骤 1：写设置测试**

```csharp
using CodexTrafficLight.Core.Models;
using CodexTrafficLight.Core.Services;

namespace CodexTrafficLight.Tests;

public sealed class AppSettingsStoreTests
{
    [Fact]
    public void LoadReturnsDefaultsWhenMissing()
    {
        var store = new AppSettingsStore(new CodexPaths(CreateTempRoot()));

        var settings = store.Load();

        Assert.Equal("dark", settings.Theme);
        Assert.Equal("triple", settings.Style);
        Assert.False(settings.Muted);
    }

    [Fact]
    public void SaveAndLoadRoundTrips()
    {
        var store = new AppSettingsStore(new CodexPaths(CreateTempRoot()));
        var expected = new AppSettings
        {
            WindowLeft = 100,
            WindowTop = 80,
            Theme = "light",
            Style = "single",
            Muted = true
        };

        store.Save(expected);
        var actual = store.Load();

        Assert.Equal(expected.WindowLeft, actual.WindowLeft);
        Assert.Equal(expected.WindowTop, actual.WindowTop);
        Assert.Equal("light", actual.Theme);
        Assert.Equal("single", actual.Style);
        Assert.True(actual.Muted);
    }

    private static string CreateTempRoot()
    {
        var path = Path.Combine(Path.GetTempPath(), "CodexTrafficLightTests", Guid.NewGuid().ToString("N"));
        Directory.CreateDirectory(path);
        return path;
    }
}
```

- [ ] **步骤 2：实现设置存储**

```csharp
using System.Text.Json;
using CodexTrafficLight.Core.Models;

namespace CodexTrafficLight.Core.Services;

public sealed class AppSettingsStore
{
    private static readonly JsonSerializerOptions JsonOptions = new()
    {
        WriteIndented = true,
        PropertyNamingPolicy = JsonNamingPolicy.CamelCase
    };

    private readonly CodexPaths _paths;

    public AppSettingsStore(CodexPaths paths)
    {
        _paths = paths;
    }

    public AppSettings Load()
    {
        try
        {
            if (!File.Exists(_paths.SettingsPath))
            {
                return new AppSettings();
            }

            return JsonSerializer.Deserialize<AppSettings>(File.ReadAllText(_paths.SettingsPath), JsonOptions)
                ?? new AppSettings();
        }
        catch
        {
            return new AppSettings();
        }
    }

    public void Save(AppSettings settings)
    {
        _paths.EnsureCodexDirectory();
        var json = JsonSerializer.Serialize(settings, JsonOptions);
        File.WriteAllText(_paths.SettingsPath, json);
    }
}
```

- [ ] **步骤 3：运行测试**

运行：

```powershell
dotnet test tests/CodexTrafficLight.Tests/CodexTrafficLight.Tests.csproj --filter AppSettingsStoreTests
```

预期结果：测试通过。

## 任务 5：实现统计和周报数据

**涉及文件：**

- 创建 `src/CodexTrafficLight.Core/Services/StatsStore.cs`
- 创建 `tests/CodexTrafficLight.Tests/StatsStoreTests.cs`

- [ ] **步骤 1：写统计测试**

```csharp
using CodexTrafficLight.Core.Models;
using CodexTrafficLight.Core.Services;

namespace CodexTrafficLight.Tests;

public sealed class StatsStoreTests
{
    [Fact]
    public void RecordsRedAndGreenCounts()
    {
        var store = new StatsStore(new CodexPaths(CreateTempRoot()));
        var day = new DateOnly(2026, 6, 1);

        store.RecordStateChange(CodexLightState.Red, CodexLightState.Unknown, null, day);
        store.RecordStateChange(CodexLightState.Green, CodexLightState.Red, DateTimeOffset.Parse("2026-06-01T10:00:00+08:00"), day, DateTimeOffset.Parse("2026-06-01T10:05:00+08:00"));

        var stats = store.Load();

        Assert.Equal(1, stats["2026-06-01"].RedCount);
        Assert.Equal(1, stats["2026-06-01"].GreenCount);
        Assert.Equal(300000, stats["2026-06-01"].RedDurationMs);
    }

    private static string CreateTempRoot()
    {
        var path = Path.Combine(Path.GetTempPath(), "CodexTrafficLightTests", Guid.NewGuid().ToString("N"));
        Directory.CreateDirectory(path);
        return path;
    }
}
```

- [ ] **步骤 2：实现统计存储**

```csharp
using System.Text.Json;
using CodexTrafficLight.Core.Models;

namespace CodexTrafficLight.Core.Services;

public sealed class StatsStore
{
    private static readonly JsonSerializerOptions JsonOptions = new()
    {
        WriteIndented = true,
        PropertyNamingPolicy = JsonNamingPolicy.CamelCase
    };

    private readonly CodexPaths _paths;

    public StatsStore(CodexPaths paths)
    {
        _paths = paths;
    }

    public Dictionary<string, DailyStats> Load()
    {
        try
        {
            if (!File.Exists(_paths.StatsPath))
            {
                return new Dictionary<string, DailyStats>();
            }

            return JsonSerializer.Deserialize<Dictionary<string, DailyStats>>(File.ReadAllText(_paths.StatsPath), JsonOptions)
                ?? new Dictionary<string, DailyStats>();
        }
        catch
        {
            return new Dictionary<string, DailyStats>();
        }
    }

    public void RecordStateChange(
        CodexLightState newState,
        CodexLightState previousState,
        DateTimeOffset? redStartedAt,
        DateOnly? localDay = null,
        DateTimeOffset? now = null)
    {
        var currentTime = now ?? DateTimeOffset.Now;
        var key = (localDay ?? DateOnly.FromDateTime(currentTime.LocalDateTime)).ToString("yyyy-MM-dd");
        var all = Load();
        all.TryGetValue(key, out var current);
        current ??= new DailyStats();

        var next = newState switch
        {
            CodexLightState.Red => current with { RedCount = current.RedCount + 1 },
            CodexLightState.Green => current with
            {
                GreenCount = current.GreenCount + 1,
                RedDurationMs = current.RedDurationMs + CalculateRedDuration(previousState, redStartedAt, currentTime)
            },
            _ => current
        };

        all[key] = next;
        Save(all);
    }

    private static long CalculateRedDuration(CodexLightState previousState, DateTimeOffset? redStartedAt, DateTimeOffset now)
    {
        if (previousState != CodexLightState.Red || redStartedAt is null)
        {
            return 0;
        }

        var duration = now - redStartedAt.Value;
        return duration < TimeSpan.Zero ? 0 : (long)duration.TotalMilliseconds;
    }

    private void Save(Dictionary<string, DailyStats> stats)
    {
        _paths.EnsureCodexDirectory();
        File.WriteAllText(_paths.StatsPath, JsonSerializer.Serialize(stats, JsonOptions));
    }
}
```

- [ ] **步骤 3：运行测试**

运行：

```powershell
dotnet test tests/CodexTrafficLight.Tests/CodexTrafficLight.Tests.csproj --filter StatsStoreTests
```

预期结果：测试通过。

## 任务 6：实现 Codex hooks 安装器

**涉及文件：**

- 创建 `src/CodexTrafficLight.Core/Services/CodexHookInstaller.cs`
- 创建 `tests/CodexTrafficLight.Tests/CodexHookInstallerTests.cs`

- [ ] **步骤 1：写 hooks 合并测试**

```csharp
using System.Text.Json;
using CodexTrafficLight.Core.Services;

namespace CodexTrafficLight.Tests;

public sealed class CodexHookInstallerTests
{
    [Fact]
    public void InstallPreservesUnrelatedHooks()
    {
        var root = CreateTempRoot();
        var paths = new CodexPaths(root);
        paths.EnsureCodexDirectory();
        File.WriteAllText(paths.HooksPath, """
        {
          "hooks": {
            "PreToolUse": [
              {
                "matcher": "Bash",
                "hooks": [
                  {
                    "type": "command",
                    "command": "echo keep-me"
                  }
                ]
              }
            ]
          }
        }
        """);

        new CodexHookInstaller(paths).InstallOrUpdate();

        var json = File.ReadAllText(paths.HooksPath);

        Assert.Contains("echo keep-me", json);
        Assert.Contains("codex_traffic_light_write_status.ps1", json);
        Assert.Contains("UserPromptSubmit", json);
        Assert.Contains("PermissionRequest", json);
        Assert.Contains("Stop", json);
        Assert.Contains("SessionStart", json);
        Assert.True(File.Exists(paths.HookScriptPath));
    }

    [Fact]
    public void InstallIsIdempotent()
    {
        var root = CreateTempRoot();
        var paths = new CodexPaths(root);
        var installer = new CodexHookInstaller(paths);

        installer.InstallOrUpdate();
        var first = File.ReadAllText(paths.HooksPath);
        installer.InstallOrUpdate();
        var second = File.ReadAllText(paths.HooksPath);

        Assert.Equal(NormalizeJson(first), NormalizeJson(second));
    }

    private static string NormalizeJson(string json)
    {
        using var doc = JsonDocument.Parse(json);
        return JsonSerializer.Serialize(doc.RootElement);
    }

    private static string CreateTempRoot()
    {
        var path = Path.Combine(Path.GetTempPath(), "CodexTrafficLightTests", Guid.NewGuid().ToString("N"));
        Directory.CreateDirectory(path);
        return path;
    }
}
```

- [ ] **步骤 2：实现 hooks 安装器**

```csharp
using System.Text.Json;
using System.Text.Json.Nodes;

namespace CodexTrafficLight.Core.Services;

public sealed class CodexHookInstaller
{
    private const string Marker = "codex_traffic_light_write_status.ps1";
    private readonly CodexPaths _paths;

    public CodexHookInstaller(CodexPaths paths)
    {
        _paths = paths;
    }

    public string InstallOrUpdate()
    {
        _paths.EnsureCodexDirectory();
        EnsureHookScript();
        var root = LoadRoot();
        var hooks = root["hooks"] as JsonObject ?? new JsonObject();
        root["hooks"] = hooks;

        AddOwnedEvent(hooks, "UserPromptSubmit", "red");
        AddOwnedEvent(hooks, "PermissionRequest", "yellow");
        AddOwnedEvent(hooks, "Stop", "green");
        AddOwnedEvent(hooks, "SessionStart", "green");

        var json = root.ToJsonString(new JsonSerializerOptions { WriteIndented = true });
        if (!File.Exists(_paths.HooksPath) || File.ReadAllText(_paths.HooksPath) != json)
        {
            File.WriteAllText(_paths.HooksPath, json);
        }
        return _paths.HooksPath;
    }

    private void EnsureHookScript()
    {
        Directory.CreateDirectory(_paths.HookScriptDirectory);
        const string script = """
        param(
            [Parameter(Mandatory=$true)]
            [ValidateSet('red','yellow','green','unknown')]
            [string]$State,

            [Parameter(Mandatory=$false)]
            [string]$EventName = 'unknown'
        )

        $codexDir = if ($env:CODEX_HOME) { $env:CODEX_HOME } else { Join-Path $env:USERPROFILE '.codex' }
        if (-not (Test-Path -LiteralPath $codexDir)) {
            New-Item -ItemType Directory -Path $codexDir -Force | Out-Null
        }

        $statusPath = Join-Path $codexDir 'codex_traffic_light_state.json'
        $payload = [ordered]@{
            state = $State
            event = $EventName
            updatedAt = (Get-Date).ToString('o')
        } | ConvertTo-Json -Compress

        Set-Content -LiteralPath $statusPath -Value $payload -Encoding UTF8
        """;

        if (!File.Exists(_paths.HookScriptPath) || File.ReadAllText(_paths.HookScriptPath) != script)
        {
            File.WriteAllText(_paths.HookScriptPath, script);
        }
    }

    private JsonObject LoadRoot()
    {
        if (!File.Exists(_paths.HooksPath))
        {
            return new JsonObject();
        }

        try
        {
            return JsonNode.Parse(File.ReadAllText(_paths.HooksPath)) as JsonObject ?? new JsonObject();
        }
        catch
        {
            var backupPath = _paths.HooksPath + ".invalid-" + DateTime.Now.ToString("yyyyMMddHHmmss") + ".bak";
            File.Copy(_paths.HooksPath, backupPath, overwrite: false);
            return new JsonObject();
        }
    }

    private void AddOwnedEvent(JsonObject hooks, string eventName, string state)
    {
        var existing = hooks[eventName] as JsonArray ?? new JsonArray();
        var cleaned = new JsonArray();

        foreach (var item in existing)
        {
            if (item is not JsonObject obj || !ContainsOwnedCommand(obj))
            {
                cleaned.Add(item?.DeepClone());
            }
        }

        cleaned.Add(CreateHookEntry(eventName, state));
        hooks[eventName] = cleaned;
    }

    private static bool ContainsOwnedCommand(JsonObject entry)
    {
        var handlers = entry["hooks"] as JsonArray;
        if (handlers is null)
        {
            return false;
        }

        return handlers.Any(handler =>
            handler is JsonObject obj &&
            obj["command"]?.GetValue<string>().Contains(Marker, StringComparison.OrdinalIgnoreCase) == true);
    }

    private JsonObject CreateHookEntry(string eventName, string state)
    {
        var command = BuildPowerShellCommand(state, eventName);
        return new JsonObject
        {
            ["hooks"] = new JsonArray
            {
                new JsonObject
                {
                    ["type"] = "command",
                    ["command"] = command,
                    ["statusMessage"] = $"Codex traffic light: {state}"
                }
            }
        };
    }

    private string BuildPowerShellCommand(string state, string eventName)
    {
        return $"powershell -NoProfile -ExecutionPolicy Bypass -File \"{_paths.HookScriptPath}\" {state} {eventName}";
    }
}
```

- [ ] **步骤 3：运行测试**

运行：

```powershell
dotnet test tests/CodexTrafficLight.Tests/CodexTrafficLight.Tests.csproj --filter CodexHookInstallerTests
```

预期结果：测试通过。

## 任务 7：实现 WPF 红绿灯界面

**涉及文件：**

- 修改 `src/CodexTrafficLight.App/MainWindow.xaml`
- 修改 `src/CodexTrafficLight.App/MainWindow.xaml.cs`

- [ ] **步骤 1：替换窗口 XAML**

```xml
<Window x:Class="CodexTrafficLight.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="100"
        Height="220"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        Topmost="True">
    <Grid>
        <Border x:Name="Body"
                Width="74"
                Height="200"
                CornerRadius="22"
                Background="#2B2B2B"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                MouseLeftButtonDown="Body_MouseLeftButtonDown">
            <Grid>
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <Ellipse x:Name="RedLamp" Width="50" Height="50" Margin="0,8" Fill="#301111"/>
                    <Ellipse x:Name="YellowLamp" Width="50" Height="50" Margin="0,8" Fill="#463411"/>
                    <Ellipse x:Name="GreenLamp" Width="50" Height="50" Margin="0,8" Fill="#0F321E"/>
                </StackPanel>
                <Button x:Name="GearButton"
                        Width="24"
                        Height="24"
                        Content="⚙"
                        FontSize="12"
                        Foreground="#D0D0D0"
                        Background="#555555"
                        BorderThickness="0"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Bottom"
                        Margin="0,0,7,7"
                        Click="GearButton_Click"/>
            </Grid>
        </Border>
    </Grid>
</Window>
```

- [ ] **步骤 2：实现灯色和闪烁动画**

在 `MainWindow.xaml.cs` 中实现：

```csharp
using System.Windows;
using System.Windows.Media;
using System.Windows.Media.Animation;
using System.Windows.Shapes;
using CodexTrafficLight.Core.Models;

namespace CodexTrafficLight.App;

public partial class MainWindow : Window
{
    private readonly Dictionary<CodexLightState, Ellipse> _lamps;

    public MainWindow()
    {
        InitializeComponent();
        _lamps = new Dictionary<CodexLightState, Ellipse>
        {
            [CodexLightState.Red] = RedLamp,
            [CodexLightState.Yellow] = YellowLamp,
            [CodexLightState.Green] = GreenLamp
        };
        ApplyState(CodexLightState.Unknown);
    }

    public void ApplyState(CodexLightState state)
    {
        StopAnimations();
        RedLamp.Fill = new SolidColorBrush(Color.FromRgb(48, 17, 17));
        YellowLamp.Fill = new SolidColorBrush(Color.FromRgb(70, 52, 17));
        GreenLamp.Fill = new SolidColorBrush(Color.FromRgb(15, 50, 30));

        if (!_lamps.TryGetValue(state, out var lamp))
        {
            return;
        }

        lamp.Fill = new SolidColorBrush(state switch
        {
            CodexLightState.Red => Color.FromRgb(190, 38, 38),
            CodexLightState.Yellow => Color.FromRgb(237, 166, 32),
            CodexLightState.Green => Color.FromRgb(21, 155, 82),
            _ => Colors.Gray
        });

        StartPulse(lamp);
    }

    private void StartPulse(Ellipse lamp)
    {
        var animation = new DoubleAnimation
        {
            From = 0.55,
            To = 1.0,
            Duration = TimeSpan.FromMilliseconds(650),
            AutoReverse = true,
            RepeatBehavior = RepeatBehavior.Forever
        };
        lamp.BeginAnimation(OpacityProperty, animation);
    }

    private void StopAnimations()
    {
        foreach (var lamp in _lamps.Values)
        {
            lamp.BeginAnimation(OpacityProperty, null);
            lamp.Opacity = 1;
        }
    }

    private void Body_MouseLeftButtonDown(object sender, System.Windows.Input.MouseButtonEventArgs e)
    {
        DragMove();
    }

    private void GearButton_Click(object sender, RoutedEventArgs e)
    {
    }
}
```

- [ ] **步骤 3：构建并启动界面**

运行：

```powershell
dotnet build CodexTrafficLight.sln
dotnet run --project src/CodexTrafficLight.App
```

预期结果：出现置顶三灯窗口，窗口可以拖动。

## 任务 8：实现状态文件监听

**涉及文件：**

- 创建 `src/CodexTrafficLight.App/StatusFileWatcher.cs`
- 修改 `src/CodexTrafficLight.App/MainWindow.xaml.cs`

- [ ] **步骤 1：创建状态文件监听器**

```csharp
using CodexTrafficLight.Core.Models;
using CodexTrafficLight.Core.Services;

namespace CodexTrafficLight.App;

public sealed class StatusFileWatcher : IDisposable
{
    private readonly StatusFileStore _store;
    private readonly FileSystemWatcher _watcher;
    private readonly System.Timers.Timer _debounce;

    public StatusFileWatcher(CodexPaths paths, StatusFileStore store)
    {
        _store = store;
        Directory.CreateDirectory(paths.CodexDirectory);
        _watcher = new FileSystemWatcher(paths.CodexDirectory, Path.GetFileName(paths.StatusPath))
        {
            NotifyFilter = NotifyFilters.LastWrite | NotifyFilters.CreationTime | NotifyFilters.Size | NotifyFilters.FileName,
            EnableRaisingEvents = true
        };
        _watcher.Changed += OnChanged;
        _watcher.Created += OnChanged;
        _watcher.Renamed += OnChanged;

        _debounce = new System.Timers.Timer(120) { AutoReset = false };
        _debounce.Elapsed += (_, _) => StatusChanged?.Invoke(_store.Read());
    }

    public event Action<CodexStatus>? StatusChanged;

    private void OnChanged(object sender, FileSystemEventArgs e)
    {
        _debounce.Stop();
        _debounce.Start();
    }

    public void Dispose()
    {
        _watcher.Dispose();
        _debounce.Dispose();
    }
}
```

- [ ] **步骤 2：在窗口中接入监听器**

在 `MainWindow` 添加字段：

```csharp
private readonly CodexPaths _paths = new();
private readonly StatusFileStore _statusStore;
private readonly StatusFileWatcher _watcher;
```

在构造函数中初始化：

```csharp
_statusStore = new StatusFileStore(_paths);
_watcher = new StatusFileWatcher(_paths, _statusStore);
_watcher.StatusChanged += status =>
{
    Dispatcher.Invoke(() => ApplyState(status.State));
};
ApplyState(_statusStore.Read().State);
```

窗口关闭时释放资源：

```csharp
protected override void OnClosed(EventArgs e)
{
    _watcher.Dispose();
    base.OnClosed(e);
}
```

- [ ] **步骤 3：手动验证状态文件变化**

启动程序：

```powershell
dotnet run --project src/CodexTrafficLight.App
```

另开 PowerShell 写入状态：

```powershell
$p="$env:USERPROFILE\.codex\codex_traffic_light_state.json"
'{"state":"red","event":"manual","updatedAt":"2026-06-01T10:45:00+08:00"}' | Set-Content -LiteralPath $p -Encoding UTF8
```

预期结果：红灯开始闪烁。

## 任务 9：实现托盘菜单

**涉及文件：**

- 修改 `src/CodexTrafficLight.App/MainWindow.xaml.cs`

- [ ] **步骤 1：添加托盘图标字段**

```csharp
private System.Windows.Forms.NotifyIcon? _notifyIcon;
```

- [ ] **步骤 2：创建托盘菜单**

```csharp
private void CreateTrayMenu()
{
    _notifyIcon = new System.Windows.Forms.NotifyIcon
    {
        Text = "Codex 红绿灯",
        Visible = true,
        Icon = System.Drawing.SystemIcons.Application,
        ContextMenuStrip = new System.Windows.Forms.ContextMenuStrip()
    };

    _notifyIcon.ContextMenuStrip.Items.Add("切换到红灯", null, (_, _) => SetManualState(CodexLightState.Red, "manual"));
    _notifyIcon.ContextMenuStrip.Items.Add("切换到黄灯", null, (_, _) => SetManualState(CodexLightState.Yellow, "manual"));
    _notifyIcon.ContextMenuStrip.Items.Add("切换到绿灯", null, (_, _) => SetManualState(CodexLightState.Green, "manual"));
    _notifyIcon.ContextMenuStrip.Items.Add(new System.Windows.Forms.ToolStripSeparator());
    _notifyIcon.ContextMenuStrip.Items.Add("切换到单灯样式", null, (_, _) => ToggleStyle());
    _notifyIcon.ContextMenuStrip.Items.Add(new System.Windows.Forms.ToolStripSeparator());
    _notifyIcon.ContextMenuStrip.Items.Add("查看配置路径", null, (_, _) => MessageBox.Show(_paths.HooksPath, "Codex hooks 配置路径"));
    _notifyIcon.ContextMenuStrip.Items.Add("重新写入配置", null, (_, _) => RewriteHooks());
    _notifyIcon.ContextMenuStrip.Items.Add(new System.Windows.Forms.ToolStripSeparator());
    _notifyIcon.ContextMenuStrip.Items.Add("切换浅色模式", null, (_, _) => ToggleTheme());
    _notifyIcon.ContextMenuStrip.Items.Add("本周周报", null, (_, _) => ShowWeeklyReport());
    _notifyIcon.ContextMenuStrip.Items.Add("检查更新", null, (_, _) => MessageBox.Show("本地版不使用在线更新检查。", "Codex 红绿灯"));
    _notifyIcon.ContextMenuStrip.Items.Add("关于我", null, (_, _) => MessageBox.Show("Codex 红绿灯，本地 Windows 版。", "关于"));
    _notifyIcon.ContextMenuStrip.Items.Add(new System.Windows.Forms.ToolStripSeparator());
    _notifyIcon.ContextMenuStrip.Items.Add("退出", null, (_, _) => System.Windows.Application.Current.Shutdown());
}
```

- [ ] **步骤 3：添加菜单动作**

```csharp
private void SetManualState(CodexLightState state, string eventName)
{
    _statusStore.Write(state, eventName);
    ApplyState(state);
}

private void RewriteHooks()
{
    var path = new CodexHookInstaller(_paths).InstallOrUpdate();
    MessageBox.Show($"Hooks 已写入：{path}\n\n请在 Codex 中运行 /hooks 并信任这些 hook。", "Codex 红绿灯");
}

private void ToggleStyle()
{
    MessageBox.Show("样式切换将在任务 10 中启用。", "Codex 红绿灯");
}

private void ToggleTheme()
{
    MessageBox.Show("主题切换将在任务 10 中启用。", "Codex 红绿灯");
}

private void ShowWeeklyReport()
{
    MessageBox.Show("本周周报将在任务 11 中启用。", "Codex 红绿灯");
}
```

这些临时方法保证任务 9 完成后项目仍然可以编译。任务 10 和任务 11 会替换为真实实现。

- [ ] **步骤 4：退出时清理托盘图标**

```csharp
protected override void OnClosed(EventArgs e)
{
    _watcher.Dispose();
    if (_notifyIcon is not null)
    {
        _notifyIcon.Visible = false;
        _notifyIcon.Dispose();
    }
    base.OnClosed(e);
}
```

- [ ] **步骤 5：手动验证托盘菜单**

运行：

```powershell
dotnet run --project src/CodexTrafficLight.App
```

预期结果：托盘图标出现，右键菜单能手动切换红黄绿。

## 任务 10：实现设置、主题、窗口位置和单灯模式

**涉及文件：**

- 修改 `src/CodexTrafficLight.App/MainWindow.xaml`
- 修改 `src/CodexTrafficLight.App/MainWindow.xaml.cs`

- [ ] **步骤 1：在界面添加设置面板**

在 `MainWindow.xaml` 的根 `Grid` 内追加：

```xml
把根 `Grid` 改成下面的布局，让设置面板位于窗口内部。齿轮打开设置时同步增加窗口高度，避免面板跑出窗口边界。

```xml
<Grid>
    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Top">
        <Border x:Name="Body"
                Width="74"
                Height="200"
                CornerRadius="22"
                Background="#2B2B2B"
                HorizontalAlignment="Center"
                MouseLeftButtonDown="Body_MouseLeftButtonDown">
            <Grid>
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <Ellipse x:Name="RedLamp" Width="50" Height="50" Margin="0,8" Fill="#301111"/>
                    <Ellipse x:Name="YellowLamp" Width="50" Height="50" Margin="0,8" Fill="#463411"/>
                    <Ellipse x:Name="GreenLamp" Width="50" Height="50" Margin="0,8" Fill="#0F321E"/>
                </StackPanel>
                <Button x:Name="GearButton"
                        Width="24"
                        Height="24"
                        Content="⚙"
                        FontSize="12"
                        Foreground="#D0D0D0"
                        Background="#555555"
                        BorderThickness="0"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Bottom"
                        Margin="0,0,7,7"
                        Click="GearButton_Click"/>
            </Grid>
        </Border>

        <Border x:Name="SettingsPanel"
                Width="100"
                Height="70"
                CornerRadius="14"
                Background="#2A2A2A"
                Margin="0,8,0,0"
                Visibility="Collapsed">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                <CheckBox x:Name="MuteCheckBox" Content="静音" Foreground="White" Margin="4" Checked="MuteCheckBox_Changed" Unchecked="MuteCheckBox_Changed"/>
                <CheckBox x:Name="ThemeCheckBox" Content="深色" Foreground="White" Margin="4" Checked="ThemeCheckBox_Changed" Unchecked="ThemeCheckBox_Changed"/>
            </StackPanel>
        </Border>
    </StackPanel>
</Grid>
```

- [ ] **步骤 2：添加设置字段**

```csharp
private AppSettingsStore? _settingsStore;
private AppSettings _settings = new();
private CodexLightState _currentState = CodexLightState.Unknown;
```

在构造函数中初始化：

```csharp
_settingsStore = new AppSettingsStore(_paths);
_settings = _settingsStore.Load();
ApplySavedWindowPosition();
MuteCheckBox.IsChecked = _settings.Muted;
ThemeCheckBox.IsChecked = _settings.Theme == "dark";
ApplyTheme(_settings.Theme);
ApplyStyle(_settings.Style);
```

- [ ] **步骤 3：实现位置保存**

```csharp
private void ApplySavedWindowPosition()
{
    if (_settings.WindowLeft.HasValue && _settings.WindowTop.HasValue)
    {
        Left = _settings.WindowLeft.Value;
        Top = _settings.WindowTop.Value;
        return;
    }

    Left = SystemParameters.WorkArea.Right - Width - 24;
    Top = SystemParameters.WorkArea.Top + 80;
}

private void SaveSettings(AppSettings settings)
{
    _settings = settings;
    _settingsStore?.Save(_settings);
}

protected override void OnLocationChanged(EventArgs e)
{
    base.OnLocationChanged(e);
    if (_settingsStore is null)
    {
        return;
    }

    SaveSettings(_settings with { WindowLeft = Left, WindowTop = Top });
}
```

- [ ] **步骤 4：实现主题切换**

```csharp
private void ToggleTheme()
{
    var next = _settings.Theme == "dark" ? "light" : "dark";
    SaveSettings(_settings with { Theme = next });
    ApplyTheme(next);
}

private void ApplyTheme(string theme)
{
    Body.Background = theme == "dark"
        ? new SolidColorBrush(Color.FromRgb(43, 43, 43))
        : new SolidColorBrush(Color.FromRgb(230, 230, 230));
}

private void ThemeCheckBox_Changed(object sender, RoutedEventArgs e)
{
    var theme = ThemeCheckBox.IsChecked == true ? "dark" : "light";
    SaveSettings(_settings with { Theme = theme });
    ApplyTheme(theme);
}
```

- [ ] **步骤 5：实现静音设置**

```csharp
private void MuteCheckBox_Changed(object sender, RoutedEventArgs e)
{
    SaveSettings(_settings with { Muted = MuteCheckBox.IsChecked == true });
}
```

- [ ] **步骤 6：实现单灯和三灯切换**

```csharp
private void ToggleStyle()
{
    var next = _settings.Style == "triple" ? "single" : "triple";
    SaveSettings(_settings with { Style = next });
    ApplyStyle(next);
}

private void ApplyStyle(string style)
{
    if (style == "single")
    {
        Body.Height = 88;
        RedLamp.Visibility = _currentState == CodexLightState.Red ? Visibility.Visible : Visibility.Collapsed;
        YellowLamp.Visibility = _currentState == CodexLightState.Yellow || _currentState == CodexLightState.Unknown ? Visibility.Visible : Visibility.Collapsed;
        GreenLamp.Visibility = _currentState == CodexLightState.Green ? Visibility.Visible : Visibility.Collapsed;
        UpdateWindowHeight();
        return;
    }

    Body.Height = 200;
    RedLamp.Visibility = Visibility.Visible;
    YellowLamp.Visibility = Visibility.Visible;
    GreenLamp.Visibility = Visibility.Visible;
    UpdateWindowHeight();
}

private void UpdateWindowHeight()
{
    var panelOpen = SettingsPanel.Visibility == Visibility.Visible;
    Height = _settings.Style == "single"
        ? (panelOpen ? 188 : 110)
        : (panelOpen ? 298 : 220);
}
```

同时在 `ApplyState` 开头设置当前状态，并在灯色更新后重新应用样式：

```csharp
_currentState = state;
```

在 `ApplyState` 的最后追加：

```csharp
ApplyStyle(_settings.Style);
```

齿轮按钮要切换设置面板并调整窗口高度：

```csharp
private void GearButton_Click(object sender, RoutedEventArgs e)
{
    SettingsPanel.Visibility = SettingsPanel.Visibility == Visibility.Visible ? Visibility.Collapsed : Visibility.Visible;
    UpdateWindowHeight();
}
```

- [ ] **步骤 7：构建并手动验证**

运行：

```powershell
dotnet build CodexTrafficLight.sln
dotnet run --project src/CodexTrafficLight.App
```

预期结果：位置、静音、主题和样式在重启后保持。

## 任务 11：实现本周周报

**涉及文件：**

- 修改 `src/CodexTrafficLight.App/MainWindow.xaml.cs`

- [ ] **步骤 1：添加统计字段**

```csharp
private readonly StatsStore _statsStore;
private CodexLightState _lastState = CodexLightState.Unknown;
private DateTimeOffset? _redStartedAt;
```

在构造函数中初始化：

```csharp
_statsStore = new StatsStore(_paths);
```

- [ ] **步骤 2：在状态变化时记录统计**

不要直接在 `ApplyState` 中统计，因为手动切灯测试也会调用 `ApplyState`。新增 `ApplyStatus`，只统计 Codex hook 产生的状态，跳过 `manual` 和 `app-start`。

```csharp
private void ApplyStatus(CodexStatus status)
{
    ApplyState(status.State);

    if (status.Event is "manual" or "app-start")
    {
        return;
    }

    if (status.State != _lastState)
    {
        _statsStore.RecordStateChange(status.State, _lastState, _redStartedAt);
        _redStartedAt = status.State == CodexLightState.Red ? DateTimeOffset.Now : null;
        _lastState = status.State;
    }
}
```

把任务 8 中 watcher 的回调改为：

```csharp
_watcher.StatusChanged += status =>
{
    Dispatcher.Invoke(() => ApplyStatus(status));
};
```

把手动切灯改成：

```csharp
private void SetManualState(CodexLightState state, string eventName)
{
    var status = new CodexStatus(state, eventName, DateTimeOffset.Now);
    _statusStore.Write(status);
    ApplyStatus(status);
}
```

- [ ] **步骤 3：实现周报弹窗**

```csharp
private void ShowWeeklyReport()
{
    var all = _statsStore.Load();
    var today = DateOnly.FromDateTime(DateTime.Now);
    var diff = ((int)today.DayOfWeek + 6) % 7;
    var monday = today.AddDays(-diff);

    var redCount = 0;
    var greenCount = 0;
    long redDuration = 0;

    for (var i = 0; i < 7; i++)
    {
        var key = monday.AddDays(i).ToString("yyyy-MM-dd");
        if (!all.TryGetValue(key, out var stats))
        {
            continue;
        }

        redCount += stats.RedCount;
        greenCount += stats.GreenCount;
        redDuration += stats.RedDurationMs;
    }

    var duration = TimeSpan.FromMilliseconds(redDuration);
    MessageBox.Show($"思考次数：{redCount} 次\n回复次数：{greenCount} 次\n思考总时长：{(int)duration.TotalHours} 小时 {duration.Minutes} 分钟", "本周周报");
}
```

- [ ] **步骤 4：手动验证周报**

运行程序，从托盘菜单切换红灯，再切换绿灯，然后打开本周周报。

预期结果：次数增加，红灯累计时长不为负数。

## 任务 12：单实例限制和启动时安装 hooks

**涉及文件：**

- 修改 `src/CodexTrafficLight.App/App.xaml.cs`
- 修改 `src/CodexTrafficLight.App/MainWindow.xaml.cs`

- [ ] **步骤 1：添加单实例限制**

在 `App.xaml.cs` 中实现：

```csharp
using System.Threading;
using System.Windows;

namespace CodexTrafficLight.App;

public partial class App : Application
{
    private Mutex? _singleInstanceMutex;
    private bool _ownsMutex;

    protected override void OnStartup(StartupEventArgs e)
    {
        _singleInstanceMutex = new Mutex(true, "CodexTrafficLight.SingleInstance", out _ownsMutex);
        if (!_ownsMutex)
        {
            Shutdown();
            return;
        }

        base.OnStartup(e);
    }

    protected override void OnExit(ExitEventArgs e)
    {
        if (_ownsMutex)
        {
            _singleInstanceMutex?.ReleaseMutex();
        }
        _singleInstanceMutex?.Dispose();
        base.OnExit(e);
    }
}
```

预期结果：重复启动 EXE 时，第二个进程立即退出，不创建第二个托盘图标。

- [ ] **步骤 2：启动时写入 hooks**

在构造函数创建托盘菜单之后执行：

```csharp
var hooksPath = new CodexHookInstaller(_paths).InstallOrUpdate();
if (!File.Exists(_paths.StatusPath))
{
    _statusStore.Write(CodexLightState.Green, "app-start");
}
```

- [ ] **步骤 3：提示用户信任 hooks**

添加提示方法：

```csharp
private void ShowHookTrustReminder(string hooksPath)
{
    MessageBox.Show($"Codex hooks 已写入：{hooksPath}\n\n请在 Codex 中运行 /hooks，并信任 Codex 红绿灯 hooks。未信任前，Codex 会跳过这些 hook。", "Codex 红绿灯");
}
```

首次安装或用户点击 `重新写入配置` 后调用该方法。

- [ ] **步骤 4：检查 hooks 文件**

运行：

```powershell
Get-Content -Raw $env:USERPROFILE\.codex\hooks.json
```

预期结果：文件中包含 `codex_traffic_light_state.json`，并且原有无关 hooks 没有被删除。

## 任务 13：补充 README

**涉及文件：**

- 创建 `README.md`

- [ ] **步骤 1：写中文说明**

```markdown
# Codex 红绿灯

这是一个纯本地 Windows 小工具，用红黄绿灯显示 Codex CLI 的运行状态。

## 环境要求

- Windows
- .NET 8 SDK
- 已启用 hooks 的 Codex CLI

## 本地运行

```powershell
dotnet run --project src/CodexTrafficLight.App
```

## 打包 EXE

```powershell
dotnet publish src/CodexTrafficLight.App/CodexTrafficLight.App.csproj -c Release -r win-x64 --self-contained true /p:PublishSingleFile=true
```

输出目录：

```text
src\CodexTrafficLight.App\bin\Release\net8.0-windows\win-x64\publish\
```

## 信任 Codex hooks

程序会把 hooks 写入 Codex 配置目录。优先使用 `CODEX_HOME`，否则使用：

```text
%USERPROFILE%\.codex\hooks.json
```

写入后，打开 Codex 并运行：

```text
/hooks
```

然后信任 Codex 红绿灯 hooks。未信任时，Codex 会跳过这些命令 hook。

## 本地文件

默认位置：

```text
%USERPROFILE%\.codex\codex_traffic_light_state.json
%USERPROFILE%\.codex\codex_traffic_light_settings.json
%USERPROFILE%\.codex\codex_traffic_light_stats.json
```

本工具不需要服务器。
```

- [ ] **步骤 2：构建验证**

运行：

```powershell
dotnet build CodexTrafficLight.sln
```

预期结果：构建成功。

## 任务 14：最终验证和打包

**涉及文件：**

- 如果验证发现问题，再按问题修改对应源文件。

- [ ] **步骤 1：运行全部测试**

运行：

```powershell
dotnet test CodexTrafficLight.sln
```

预期结果：全部测试通过。

- [ ] **步骤 2：发布单文件 EXE**

运行：

```powershell
dotnet publish src/CodexTrafficLight.App/CodexTrafficLight.App.csproj -c Release -r win-x64 --self-contained true /p:PublishSingleFile=true
```

预期结果：发布成功，并生成 `CodexTrafficLight.App.exe`。

- [ ] **步骤 3：手动冒烟测试**

从发布目录运行 EXE：

```text
src\CodexTrafficLight.App\bin\Release\net8.0-windows\win-x64\publish\
```

预期结果：

- 悬浮红绿灯出现。
- 托盘图标出现。
- 托盘菜单可以手动切换红黄绿。
- `重新写入配置` 能写入 hooks。
- `查看配置路径` 显示 `%USERPROFILE%\.codex\hooks.json`。
- `本周周报` 可以打开。
- `退出` 能关闭窗口并移除托盘图标。

- [ ] **步骤 4：Codex 集成测试**

在 Codex 中运行：

```text
/hooks
```

信任新 hook 后，发送一个简单任务。

预期结果：

- 提交任务后变红灯。
- 当前轮次结束后变绿灯。
- Codex 请求权限时变黄灯。

## 自检结果

- 需求覆盖：计划覆盖 WPF 悬浮界面、托盘菜单、hooks 安装、本地状态文件、设置、统计、本地更新提示、测试和打包。
- 占位检查：没有未解决的占位标记或未完成步骤。
- 类型一致性：`CodexLightState`、`CodexStatus`、`CodexPaths`、`StatusFileStore`、`AppSettingsStore`、`StatsStore`、`CodexHookInstaller` 都在被后续任务引用前先定义。

## 2026-06-01 当前完成版记录

当前完成版发布路径：

```text
C:\Users\28022\Desktop\codex\dist\CodexTrafficLight-plugin-session-fix\CodexTrafficLight.App.exe
```

已完成范围：

- WPF 悬浮红黄绿灯、单灯/三灯样式、深浅色切换、窗口位置保存。
- 托盘菜单：显示/隐藏、手动切灯、样式切换、主题切换、查看配置路径、重新写入配置、黄灯自动展开开关、显示/隐藏已结束会话、清理已完成会话、本周周报、检查更新、关于我、退出。
- 启动时安装/更新 Codex hooks，并提示用户信任 hooks。
- 本地状态文件、设置文件、统计文件、每会话状态文件和诊断文件。
- 单实例限制，避免重复启动多个托盘图标。
- EXE 和托盘图标已替换为项目图标。
- “关于我”弹窗已更新为状态说明、技术栈、纯本地说明、励志语和作者 `Gyk`。
- `检查更新` 已改为手动远程检查：读取 HTTPS `version.json`，发现新版时提示更新内容并询问是否打开 GitHub Release 页面，不自动下载安装。
- README 已说明本地运行、打包、hooks 信任、多会话模式和备用 CLI 启动器。

验证记录：

- `dotnet test CodexTrafficLight.sln --no-restore` 已通过。
- `dotnet build CodexTrafficLight.sln --no-restore` 已通过。
- `dotnet publish src\CodexTrafficLight.App\CodexTrafficLight.App.csproj -c Release -r win-x64 --self-contained true /p:PublishSingleFile=true -o dist\CodexTrafficLight-plugin-session-fix` 已成功发布。
- 用户已确认 CLI 和 VS Code 插件多任务核心场景可用。

保留限制：

- VS Code 插件缺少单个聊天关闭事件，不能实时知道某个旧聊天已关闭；当前用短时隐藏规则兜底：黄灯 5 分钟、红灯 10 分钟、绿灯 5 分钟。
- 计划文档中的早期 `[ ]` 复选框保留为原始实施路线记录，本节作为当前真实状态来源。
